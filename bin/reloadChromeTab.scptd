tell application "Google Chrome"
	reload (tabs of window 1 whose URL contains "localhost:2222")
end tell

# add current time to message
set message to (time string of (current date)) & " refreshed Chrome"
return message